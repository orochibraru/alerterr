{
	"name": "homelab-alerter",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run --hot ./index.ts",
		"schema": "bun run generate-schema.ts",
		"lint": "biome check .",
		"lint:fix": "biome check --fix .",
		"gen:schema": "bun run generate-schema.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.1",
		"@types/bun": "^1.3.14",
		"@types/node": "^26.0.1"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"systeminformation": "^5.31.11",
		"zod": "^4.4.3"
	}
}
